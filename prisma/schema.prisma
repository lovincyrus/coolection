// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["postgresqlExtensions"]
}

datasource db {
  provider   = "postgresql"
  url        = env("DATABASE_URL")
  extensions = [vector]
}

model Item {
  id          String                       @id @default(uuid())
  url         String? // For content that has a URL
  title       String // Common title field for all content types
  description String? // A brief description or summary of the content
  content     String? // The main content body, e.g., the text of a tweet or article
  image       String? // An optional image URL for content with images
  createdAt   DateTime                     @default(now())
  updatedAt   DateTime                     @updatedAt
  deletedAt   DateTime? // For soft deletion
  isDeleted   Boolean                      @default(false) // For soft deletion
  type        String                       @default("website") // "website", "tweet"
  metadata    Json? // A JSON field to store any additional structured data relevant to the item type
  embedding   Unsupported("vector(1536)")? // A field to store the vector embedding of the content

  // Relations
  lists List[] @relation("ItemsOnList")

  @@map("item")
}

model List {
  id          String    @id @default(uuid())
  name        String
  description String?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  deletedAt   DateTime? // For soft deletion
  isDeleted   Boolean   @default(false) // For soft deletion
  items       Item[]    @relation("ItemsOnList")

  @@map("list")
}
